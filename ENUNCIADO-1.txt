== CONTEXTO ==

Este enunciado expande a linguagem TIL. As alterações são realizadas sobre a
versão de 2024/05/31 17:00. O teste realiza-se estritamente de forma individual
(violações conduzem a reprovação imediata).

Se aquela versão do projecto não contiver algum aspecto necessário para a
resolução do teste prático, toda a implementação deve ser realizada no contexto
do teste, como se da execução do projecto se tratasse. A não existência de
alguns aspectos não é limitação intrínseca à implementação deste teste.

== NOVA INSTRUÇÃO A ADICIONAR AO COMPILADOR ==

A instrução seguinte permite executar uma função sobre cada elemento de uma
zona de memória indexável por um ponteiro. A função a aplicar deve ter um
único argumento do mesmo tipo dos elementos indexados. Além da função e do
ponteiro para o início da zona de memória, a instrução aceita as expressões
que correspondem aos índices dos extremos do intervalo a processar.

     (apply <function> <vector> <low> <high>)

Os elementos entre < e > denotam partes variáveis da instrução.
Note-se que todas as partes variáveis da instrução podem ser construídas por
qualquer forma apropriada para elas na linguagem TIL.

Altere o compilador para que suporte esta instrução (especificações Flex e
Bison, criação de nós, processamento semântico e geração de código).

== EXEMPLO ==

(var show (function (void (double element)) (println element)))
(var f
     (function (void (double! vector) (int size))
        /* "show" aplicado a elementos de "vector" */
        (apply show (+ vector 1) (+ 2 1) (- size 1))
))

